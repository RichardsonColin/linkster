<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <% include ./partials/_header.ejs %>
  <h2>URLS And Their Shorthand</h2>
    <ul>
      <% for (var url in urls) { %>
        <li><%= `${urls[url]} - ${url}` %></li>
      <% } %>
    </ul>
    <a href="#">Change a URL</a>
</body>
</html>